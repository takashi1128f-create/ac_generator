ターミナルコマンド
git checkout ee867d8fb9796c8ebbe348b09d026700020d8257 .
git commit -m "⁠alpha・beta・無印で認識に戻"
git push origin main

一緒にやる
npm install && npm run build

GitHubでソースコードをアップロードしました。
コメットIDは「e6b9b894757f97401a34cf380b88414957590390」です。
現状の確認と整合性、繋がりの全てを網羅して下さい。
中途半端に確認をされると動作不良になります。

修正コードで修正前のコードが合致しない場合、100％に事実が無い場合は修正はしません。
そして、過去に使用した関数・変数で使用できる物は必ず使用してください。
無理やりやるようなことはソースコードを作成する上で絶対にあってはならないのでやめてください。
Javascript初心者の私に修正する為に行数では分からないので上の行のコードと下の行のコードを提示して分かりやすく解説してください。


Electron側の仕様
⁠electron-builder⁠ の仕様（⁠detectUpdateChannel⁠）により、GitHubパブリッシュ時にはバージョン名の末尾からチャンネルを自動推論しない仕組みになっています。
⁠electron-updater⁠ がデフォルトで標準チャンネル（プレリリース用のルート）として認識・処理できる識別子はalpha⁠、beta⁠、rc⁠のみです。
そのため、⁠-pre⁠ などのカスタム文字列を認識させるには、GitHub側ではなく、Electronアプリのコード側で ⁠autoUpdater.channel = 'pre'⁠ のように明示的にチャンネルを指定する必要があります。

betaは「Latest」「Pre-release」両方だが、任意のアップデートなのでテストで開発者が手動で扱う。
無印は「Latest」のみのアップデート。

一般的な考え
パッチバージョン（3桁目）: UIの微修正、軽微なバグ修正、依存関係の更新など、機能そのものに変更がない場合に使用します。
マイナーバージョン（2桁目）: 後方互換性がある状態での機能追加を行う場合に使用します。
メジャーバージョン（1桁目）: 後方互換性のない破壊的な変更を行う場合に使用します。

1桁目：読み込みファイルの追加
2桁目：機能追加
3桁目：バグ修正、CSS調整